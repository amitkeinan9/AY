import { css } from "@emotion/react";

export const postHeaderStyles = css({
  display: "flex",
  flexDirection: "row",
  fontSize: "1.5rem",
  alignItems: "center",
  justifyContent: 'space-between', 
  paddingRight: 10
});

export const actionPostContainerStyles = css({
  display: "flex",
  gap: '0.2rem',
});

export const actionPostButtonStyles = css({
  fontSize: '0.8rem',  
});

export const backButtonStyles = css({
  margin: 10,
});
