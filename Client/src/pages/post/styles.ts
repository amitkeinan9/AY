import { css } from "@emotion/react";

export const postHeaderStyles = css({
  display: "flex",
  flexDirection: "row",
  fontSize: "1.5rem",
  alignItems: "center",
});

export const backButtonStyles = css({
  margin: 10,
});
